# 灵迈单案件一次性收费后端交接说明

> 目标读者：后端开发 agent
> 业务版本：2026-09-23

## 产品规则

灵迈帮助用户独立完成标准交通事故处理，不设置律师咨询或律师导流入口。一个事故案件只支付一次，当前默认价格为 49.90 元人民币；支付后解锁该案件的完整报告、赔偿测算、行动清单、文书生成和后续重新分析。补充材料和重新生成内容不重复收费，只有创建新的独立案件时重新收费。

## 数据模型

新增 `case_entitlements`：

- `id`
- `case_id`（唯一）
- `user_id`
- `status`：`unpaid | pending | paid | refunded`
- `amount_cents`：默认 `4990`
- `currency`：`CNY`
- `paid_at`、`created_at`、`updated_at`

新增 `payment_orders`：

- `id`
- `order_no`（唯一）
- `case_id`
- `user_id`
- `amount_cents`
- `currency`
- `status`：`created | pending | paid | failed | closed | refunded`
- `wechat_transaction_id`（可空且唯一）
- `prepay_id`（可空）
- `idempotency_key`（可空）
- `created_at`、`paid_at`、`updated_at`

必须建立 `case_entitlements.case_id`、`payment_orders.order_no`、`payment_orders.wechat_transaction_id` 唯一约束，并校验案件归属。

## API

### 查询权益

```http
GET /api/cases/current/entitlement
Authorization: Bearer <jwt>
```

```json
{
  "caseId": "case_123",
  "status": "unpaid",
  "amountCents": 4990,
  "currency": "CNY",
  "paidAt": null,
  "features": {
    "fullReport": false,
    "compensationEstimate": false,
    "actionPlan": false,
    "documentGeneration": false,
    "rerunAfterMaterialUpdate": false
  }
}
```

### 创建支付订单

```http
POST /api/cases/:caseId/payment-orders
Authorization: Bearer <jwt>
Idempotency-Key: <client-generated-key>
Content-Type: application/json
```

请求体只需允许 `currency: CNY`，金额必须从服务端配置读取，不能信任客户端金额。

未支付返回微信 `requestPayment` 所需的 `timeStamp`、`nonceStr`、`package`、`signType`、`paySign`；已支付直接返回 `alreadyPaid: true` 和权益。

### 查询订单

```http
GET /api/payment-orders/:orderId
Authorization: Bearer <jwt>
```

只能查询当前用户自己的订单，并返回订单状态与案件权益状态。

### 微信回调

```http
POST /api/payments/wechat/notify
```

必须验签、核对商户号、订单号、用户、案件、金额和币种。在事务中同时更新订单和案件权益；重复通知返回成功但不得重复发放权益。开发环境可以抽象 mock provider，生产环境不能伪造支付成功。

## 完整能力权限

未支付时以下能力返回 HTTP 402：

- 完整报告生成/读取
- 完整赔偿测算
- 行动清单
- 文书生成
- 材料变化后的完整重新分析

```json
{
  "error": "PAYMENT_REQUIRED",
  "message": "请先解锁本案件的完整处理方案",
  "caseId": "case_123",
  "amountCents": 4990,
  "currency": "CNY"
}
```

免费能力继续可用：创建案件、上传材料、OCR、字段提取、确认材料和基础缺失材料提示。请将权益判断抽成可复用 service/middleware。

## 案件响应扩展

`GET /api/cases/current` 建议返回：

```json
{
  "id": "case_123",
  "title": "香港路追尾事故",
  "status": "processing",
  "statusLabel": "材料整理中",
  "materialCount": 3,
  "hasReport": false,
  "entitlement": {
    "status": "unpaid",
    "amountCents": 4990,
    "currency": "CNY",
    "paidAt": null
  },
  "nextAction": "continue_materials"
}
```

`nextAction` 可取：`create_case`、`continue_materials`、`confirm_materials`、`unlock_full_plan`、`view_report`、`continue_case`。

## 安全、幂等和测试

必须覆盖：

- JWT、案件归属和订单归属校验
- 客户端篡改金额不能影响服务端价格
- 同一案件重复创建订单不会重复扣款
- 相同幂等键只创建一个订单
- 微信回调重复通知不会重复发放权益
- 支付取消不会变成 `paid`
- 已付费案件补充材料和重新分析不再创建订单
- 新案件不继承旧案件权益
- 回调金额不一致时拒绝确认
- 事务失败时订单和权益不能半成功

交付时请返回：修改文件、迁移命令、接口列表、测试命令和结果、微信支付环境变量，以及前端调用支付的示例。不要实现订阅制、多次收费或律师咨询导流。
